let msg = "sahil";
console.log(msg.slice(2, msg.length));
console.log(msg.slice(-1)); // 5 - 1 = 4. it'll print l

// replace
let message = "ILoveCoding";
message.replace("Love", "Do");

message.replace('I', 'U');

// repeat
let str = "Sahil";
str.repeat(3);  // SahilSahilSahil